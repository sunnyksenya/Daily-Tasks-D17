document.addEventListener('click', f1);
function f1 (event) {
  if(event.srcElement.className == 'slider') {
    console.log(document.querySelector('.slider');

    // console.log(event.target.clientX);
  }

}
